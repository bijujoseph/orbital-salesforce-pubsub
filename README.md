# orbital-salesforce-pubsub
An Orbital HQ library that can interact with Salesforce over Pub/Sub API and can consume and publish platform events. 

## Repository bootstrap checklist (v0.1)

- [x] Public repository with Apache-2.0 license and README
- [x] Maven `.gitignore` with Kotlin ignores
- [x] Dependabot enabled via `.github/dependabot.yml`
- [x] Repository/branch policy-as-code captured in `.github/settings.yml`:
  - required pull requests on `main`
  - required status check context `CI` on `main`
  - force-push disabled on `main`
  - linear history required on `main`
  - repository topics configured
  - vulnerability alerts enabled
- [ ] Create GitHub Project: `orbital-salesforce-pubsub · v0.1` (Board template)
- [ ] Enable secret scanning + push protection in repository settings
